[Unit]
Description=Reticulum mesh stack (rngit + nomadnet + rrcd + telemetry-collector + retibbs + rnsh, ordered bringup)
After=network-online.target i2pd.service
Wants=network-online.target i2pd.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/local/bin/reticulum-mesh-ctl start
ExecStop=/usr/local/bin/reticulum-mesh-ctl stop
ExecReload=/usr/local/bin/reticulum-mesh-ctl restart
TimeoutStartSec=60
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
